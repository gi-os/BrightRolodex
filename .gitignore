*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
.kotlin

# NOTE: keystore/*.jks is deliberately NOT ignored here, unlike BrightCollect.
# See keystore/README.md — the signing key is committed on purpose in this family.
